import { EmploymentCreateView } from "@/sections/employment/view";

// ----------------------------------------------------------------------

export const metadata = {
  title: "雇用形態マスタ作成",
};

export default function EmploymentCreatePage() {
  return <EmploymentCreateView />;
}
