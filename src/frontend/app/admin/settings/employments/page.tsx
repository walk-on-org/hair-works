import { EmploymentListView } from "@/sections/employment/view";

// ----------------------------------------------------------------------

export const metadata = {
  title: "雇用形態マスタ一覧",
};

export default function EmploymentListPage() {
  return <EmploymentListView />;
}
