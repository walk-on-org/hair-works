import { PlanCreateView } from "@/sections/plan/view";

// ----------------------------------------------------------------------

export const metadata = {
  title: "プランマスタ作成",
};

export default function PlanCreatePage() {
  return <PlanCreateView />;
}
