import { CityListView } from "@/sections/city/view";

// ----------------------------------------------------------------------

export const metadata = {
  title: "市区町村マスタ一覧",
};

export default function CityListPage() {
  return <CityListView />;
}
