import { CityCreateView } from "@/sections/city/view";

// ----------------------------------------------------------------------

export const metadata = {
  title: "市区町村マスタ作成",
};

export default function CityCreatePage() {
  return <CityCreateView />;
}
