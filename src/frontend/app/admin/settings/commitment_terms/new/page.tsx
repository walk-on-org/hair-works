import { CommitmentTermCreateView } from "@/sections/commitment-term/view";

// ----------------------------------------------------------------------

export const metadata = {
  title: "こだわり条件マスタ作成",
};

export default function CommitmentTermCreatePage() {
  return <CommitmentTermCreateView />;
}
