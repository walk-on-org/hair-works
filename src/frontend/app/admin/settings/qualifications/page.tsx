import { QualificationListView } from "@/sections/qualification/view";

// ----------------------------------------------------------------------

export const metadata = {
  title: "保有資格マスタ一覧",
};

export default function QualificationListPage() {
  return <QualificationListView />;
}
