import { QualificationCreateView } from "@/sections/qualification/view";

// ----------------------------------------------------------------------

export const metadata = {
  title: "保有資格マスタ作成",
};

export default function QualificationCreatePage() {
  return <QualificationCreateView />;
}
