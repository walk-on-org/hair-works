import { HolidayListView } from "@/sections/holiday/view";

// ----------------------------------------------------------------------

export const metadata = {
  title: "休日マスタ一覧",
};

export default function HolidayListPage() {
  return <HolidayListView />;
}
