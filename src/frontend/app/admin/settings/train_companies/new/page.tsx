import { TrainCompanyCreateView } from "@/sections/train-company/view";

// ----------------------------------------------------------------------

export const metadata = {
  title: "鉄道事業者マスタ作成",
};

export default function TrainCompanyCreatePage() {
  return <TrainCompanyCreateView />;
}
