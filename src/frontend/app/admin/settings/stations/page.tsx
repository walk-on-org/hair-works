import { StationListView } from "@/sections/station/view";

// ----------------------------------------------------------------------

export const metadata = {
  title: "駅マスタ一覧",
};

export default function StationListPage() {
  return <StationListView />;
}
