import { StationCreateView } from "@/sections/station/view";

// ----------------------------------------------------------------------

export const metadata = {
  title: "駅マスタ作成",
};

export default function StationCreatePage() {
  return <StationCreateView />;
}
