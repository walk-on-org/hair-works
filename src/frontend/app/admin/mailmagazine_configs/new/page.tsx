import { MailmagazineConfigCreateView } from "@/sections/mailmagazine-config/view";

// ----------------------------------------------------------------------

export const metadata = {
  title: "メルマガ設定作成",
};

export default function MailmagazineConfigCreatePage() {
  return <MailmagazineConfigCreateView />;
}
