import { OfficeCreateView } from "@/sections/office/view";

// ----------------------------------------------------------------------

export const metadata = {
  title: "事業所作成",
};

export default function OfficeCreatePage() {
  return <OfficeCreateView />;
}
